import test from 'node:test';
import assert from 'node:assert/strict';
import { normalizedTaskEnvelope, taskEnvelopeHash, validateTaskEnvelope } from '../src/contracts.mjs';
import { sampleTask, tempWorkspace } from './helpers.mjs';
test('valid task envelope passes and hashes',async()=>{const w=await tempWorkspace();const t=sampleTask(w);assert.equal(validateTaskEnvelope(t),t);assert.match(taskEnvelopeHash(t),/^[0-9a-f]{64}$/u);});
test('normalization sorts set-like fields',async()=>{const w=await tempWorkspace();const t=sampleTask(w,{allowed_paths:['tests/**','src/**','src/**']});assert.deepEqual(normalizedTaskEnvelope(t).allowed_paths,['src/**','tests/**']);});
for(const [name,patch,pattern] of [['unknown schema',{schema_version:'x'},/unsupported/u],['bad role',{role:'coder'},/invalid role/u],['bad mode',{mode:'WRITE'},/invalid mode/u],['bad risk',{risk:'R9'},/invalid risk/u],['expired',{created_at:'2025-01-01T00:00:00.000Z',expires_at:'2026-01-01T00:00:00.000Z'},/expired/u],['invalid lifetime',{created_at:'2026-08-13T00:00:00.000Z',expires_at:'2026-01-01T00:00:00.000Z'},/later than created_at/u],['missing sandbox',{execution:{sandbox_verified:false}},/verified sandbox/u],['unqualified harness',{execution:{qualified:false}},/qualified harness/u],['nested agents',{delegation:{allow_nested_agents:true}},/nested agents/u],['fanout too high',{delegation:{max_parallel_agents:5}},/between 1 and 4/u],['required command not allowed',{required_test_commands:['npm run security']},/not allowed/u],['read-only writer',{mode:'REVIEW'},/writer role requires EXECUTE/u],['writerless execute',{role:'reviewer'},/restricted to writer/u],['duplicate acceptance',{acceptance_criteria:[{id:'AC1',statement:'a',verification:'a'},{id:'AC1',statement:'b',verification:'b'}]},/duplicate/u],['unexpected key',{surprise:true},/unexpected keys/u]])test(`task rejects ${name}`,async()=>{const w=await tempWorkspace();assert.throws(()=>validateTaskEnvelope(sampleTask(w,patch)),pattern);});
